# Copy to supabase/.env and fill in, then `npm run db:stop && npm run db:start`.
# Reuse the existing Google OAuth client; add this redirect URI to it:
#   http://127.0.0.1:54821/auth/v1/callback
SUPABASE_AUTH_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_GOOGLE_SECRET=
